<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bidzis.rsystem">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".User.RegisterUserActivity" />
        <activity android:name=".Ticket.SendTicketActivity" />
        <activity android:name=".User.UserManagementActivity" />
        <activity android:name=".UserSettings.UserSettingsActivity" />
        <activity android:name=".User.ShowAllUsersActivity" />
        <activity android:name=".User.GetUserByActivityActivity" />
        <activity android:name=".User.GetUserByNameAndSurnameActivity" />
        <activity android:name=".User.GetUsersByRoleActivity" />
        <activity android:name=".User.RemoveUsersActivity" />
        <activity android:name=".User.SaveUserActivity" />
        <activity android:name=".User.ChangeUserActiveActivity" />
        <activity android:name=".User.ChangeUserActiveWindowActivity" />
        <activity android:name=".UserSettings.ChangeEmailActivity" />
        <activity android:name=".UserSettings.ChangeLoginActivity" />
        <activity android:name=".UserSettings.ChangePasswordActivity" />
        <activity android:name=".User.UpdateProjectForUserActivity" />
        <activity android:name=".User.UpdateProjectForUserProjectListActivity" />
        <activity
            android:name=".User.UserSiteActivity"
            android:label="@string/title_activity_user_site"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>